## String Builder Class 
In java String Buffer is synchronous class that makes string buffer performance slow becuase in synchronous no two threads can execute together.

So to overcome with this java introduced String Builder Class in JDK 1.5 version. All the methods of Strig Builder class are non-synchronized. 

All the methods and constructors present inside String Buffer Class are also present inside String Builder Class.