/**
 * Concat Method is used to join the two strings. Instead of usig concat() method we can use "+" operator to joi the two strings
 */
public class ConcatMethod {

    public static void main(String[] args) {
        
        String str1 = "Welcome to";
        String str2 = " the jungle";

        System.out.println(str1.concat(str2));
    }
}